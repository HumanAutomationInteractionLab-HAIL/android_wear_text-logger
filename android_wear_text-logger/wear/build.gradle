apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyAlias 'smartwatch text log file'
            keyPassword 'hejibo12'
            storePassword 'hejibo12'
            storeFile file('/Users/hejibo/OneDrive/Research/Wear typing/keypath')
        }
    }
    compileSdkVersion 25
    buildToolsVersion '26.0.2'
    defaultConfig {
        applicationId "com.krzysztofwitczak.androwearapp"
        minSdkVersion 21
        targetSdkVersion 25
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.google.android.support:wearable:2.0.3'
    compile 'com.google.android.gms:play-services-wearable:11.0.0'
    provided 'com.google.android.wearable:wearable:2.0.3'
}
